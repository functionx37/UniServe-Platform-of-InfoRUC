# 同学 D 交付说明

我已在当前目录直接生成同学 D 负责部分的可交付内容，结构如下：

## 目录说明

- `admin-frontend/`
  - React + TypeScript 管理后台代码
  - 已实现侧边栏结构、批量导入导出、精准推送、联调测试说明页面
  - 包含 `Dockerfile` 与 `nginx.conf`
- `postman/`
  - 管理后台接口测试集合
- `qa/`
  - `k6` 压测脚本
  - 压力测试报告模板
- `管理后台接口对齐说明.md`
  - 后续后端同学按此文档实现即可与前端对齐
- `后续同学接手说明.md`
  - 明确谁该看什么、按什么顺序看、真正只需要改哪个文件

## 你负责的需求映射

1. `PC 管理后台`
   - 已完成基础布局与教师端可视化页面。
2. `F-14 数据导入与导出`
   - 已完成 Excel / CSV 导入与 Excel 导出功能。
3. `F-10 / F-11 标签推送`
   - 已完成标签筛选、命中对象预览、消息发送日志的前端演示逻辑。
4. `质量保证`
   - 已补充 Docker 部署文件、Postman 接口集合、压力测试脚本与报告模板。
5. `接口对齐`
   - 已补充统一接口契约文档，便于后端后续直接对接。
6. `交接友好`
   - 已补充接手说明，后续同学可直接按阅读顺序继续开发。

## 运行方式

### 前端运行

```bash
cd admin-frontend
npm install
npm run dev
```

### Docker 运行

```bash
cd admin-frontend
docker build -t rucapp-admin .
docker run -p 8080:80 rucapp-admin
```

## 还需要你手动做的事

1. 把 `后续同学接手说明.md` 和 `管理后台接口对齐说明.md` 一起发给后续开发同学。
2. 后端开始开发后，只需要改 `admin-frontend/src/api/adminApi.ts`，页面层基本不用动。
3. 如果答辩需要截图，启动页面后把四个菜单都截图。
4. 如果老师要求“真实联调记录”，就用我给你的 Postman 集合把请求地址改成真实服务地址再跑一遍。
