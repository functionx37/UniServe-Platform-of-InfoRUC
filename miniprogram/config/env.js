const BASE_URL = "http://127.0.0.1:8081"
const USE_MOCK = false
const TIMEOUT = 10000

module.exports = {
  BASE_URL,
  USE_MOCK,
  TIMEOUT,
  baseURL: BASE_URL,
  useMock: USE_MOCK,
  timeout: TIMEOUT
}
