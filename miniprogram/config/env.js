const BASE_URL = "http://10.10.0.9:8081"
const USE_MOCK = false
const TIMEOUT = 10000

module.exports = {
  BASE_URL,
  USE_MOCK,
  TIMEOUT,
  baseURL: BASE_URL,
  useMock: USE_MOCK,
  timeout: TIMEOUT
}
